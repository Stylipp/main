---
phase: 06-swipe-interface-feedback
plan: 05
type: execute
depends_on: ["06-04"]
files_modified:
  - apps/stylipp.com/src/features/feed/pages/FeedPage.tsx
  - apps/stylipp.com/src/features/feed/components/FeedEmptyState.tsx
  - apps/stylipp.com/src/App.tsx
---

<objective>
Assemble the complete Feed page, integrate all swipe components, handle edge states, and verify the full swipe flow.

Purpose: Wire everything together into the /feed route — the main user-facing experience after onboarding.
Output: Working /feed page with swipe cards, button controls, undo, loading/empty states. Phase 6 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-swipe-interface-feedback/6-RESEARCH.md

@apps/stylipp.com/src/App.tsx
@apps/stylipp.com/src/features/feed/components/SwipeCardStack.tsx
@apps/stylipp.com/src/features/feed/components/SwipeActions.tsx
@apps/stylipp.com/src/features/feed/hooks/useFeed.ts
@apps/stylipp.com/src/features/feed/hooks/useFeedbackSubmit.ts
@apps/stylipp.com/src/features/feed/stores/swipeStore.ts
@apps/stylipp.com/src/theme.ts

**Tech stack available:** React 19, framer-motion ^12.30.0, MUI ^7.3.7, Zustand ^5.0.11, react-router-dom ^7.13.0
**Established patterns:**
- OnboardingGate wraps /feed route (auth + onboarding required)
- CalibrationStep has loading skeleton pattern with MUI Skeleton
- App.tsx routing pattern with PrivateRoute + OnboardingGate nesting
**Constraining decisions:**
- Phase 04-04: OnboardingGate redirects to /onboarding if onboarding not completed
- Phase 06 Research: Prefetch next batch when 5 cards remain
- Phase 06 Research: Show skeleton cards while loading (avoid flicker to empty state)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeedPage and integrate into App routing</name>
  <files>apps/stylipp.com/src/features/feed/pages/FeedPage.tsx, apps/stylipp.com/src/App.tsx</files>
  <action>
**FeedPage.tsx:**
Assemble the complete feed experience page:

Layout structure (vertical, centered, full viewport):
- Header area: app logo/name (Typography "Stylipp" or minimal branding), user avatar or settings icon (optional, keep minimal)
- Main area: SwipeCardStack component (flex-grow, centered)
- Actions area: SwipeActions component (fixed at bottom, above safe area)

Wire up hooks and state:
1. Call useFeed() — triggers initial fetch, manages prefetch
2. Call useFeedbackSubmit() — provides submitFeedback and undoLastSwipe
3. Read currentCard, remainingCards, canUndo from useSwipeStore
4. Pass onSwipe callback to SwipeCardStack that calls submitFeedback
5. Pass onLike/onDislike/onSave/onUndo + canUndo to SwipeActions
6. Save action: call submitFeedback(productId, 'save') without advancing the card — save is non-exclusive (can like AND save)

Loading state:
- While initial feed loads (isLoading && cards.length === 0): show 1-2 skeleton cards (MUI Skeleton with similar card dimensions) + disabled action buttons
- While prefetching next batch: show subtle loading indicator (e.g., small CircularProgress at top)

Error state:
- If error from useFeed: show error message with retry button (Box centered with Typography + Button)

**App.tsx:**
Replace the placeholder `<div>Feed coming in Phase 5</div>` with:
- Import FeedPage from './features/feed/pages/FeedPage'
- `<Route path="/feed" element={<FeedPage />} />`

Keep the existing PrivateRoute + OnboardingGate wrappers — they already protect /feed.

Cleanup swipe store on unmount: useEffect cleanup calling swipeStore.reset() when FeedPage unmounts.
  </action>
  <verify>cd apps/stylipp.com && npx tsc --noEmit src/features/feed/pages/FeedPage.tsx src/App.tsx 2>&1 | head -20</verify>
  <done>FeedPage renders with SwipeCardStack + SwipeActions, App.tsx routes /feed to FeedPage, loading/error states handled</done>
</task>

<task type="auto">
  <name>Task 2: Add empty state and undo integration</name>
  <files>apps/stylipp.com/src/features/feed/components/FeedEmptyState.tsx, apps/stylipp.com/src/features/feed/pages/FeedPage.tsx</files>
  <action>
**FeedEmptyState.tsx:**
Component shown when all cards have been swiped and no more are available.

Content:
- Illustration area: use a large MUI icon (CheckCircleOutlineIcon or ExploreIcon) or simple styled emoji as placeholder
- Heading: "You've seen everything!" (Typography variant h5)
- Subtext: "Check back later for new recommendations" (Typography variant body2, color text.secondary)
- Action button: "Refresh Feed" (Button variant outlined) that calls useFeed().refetch()

Style: centered vertically in the card stack area, matching card dimensions.

**FeedPage.tsx updates:**
1. Import FeedEmptyState
2. Add conditional rendering: if remainingCards === 0 AND !hasMore AND !isLoading → show FeedEmptyState instead of SwipeCardStack
3. Wire undo properly:
   - SwipeActions onUndo calls useFeedbackSubmit().undoLastSwipe()
   - After undo, the card reappears in the stack (currentIndex decremented by store)
   - Disable undo button during animation (use a brief isAnimating state, 300ms)
4. Save button behavior:
   - Save does NOT advance the card — it bookmarks the current card
   - After save, show brief visual confirmation (e.g., BookmarkIcon scale animation or snackbar "Saved!")
   - The card remains on top for like/dislike decision
  </action>
  <verify>cd apps/stylipp.com && npx tsc --noEmit src/features/feed/components/FeedEmptyState.tsx src/features/feed/pages/FeedPage.tsx 2>&1 | head -20</verify>
  <done>Empty state shows when feed exhausted with refresh button, undo decrements index and reappears card, save bookmarks without advancing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete swipe interface: draggable cards with gesture physics, LIKE/NOPE indicators, button controls, undo, feedback persistence, feed pagination, and empty state</what-built>
  <how-to-verify>
    1. Start backend: cd apps/backend && docker compose up -d && uvicorn src.main:app --reload
    2. Start frontend: cd apps/stylipp.com && npm run dev
    3. Log in (or register) and complete onboarding if not done
    4. Navigate to /feed — should see a stack of product cards
    5. **Swipe right** on a card — card should fly right with green LIKE stamp visible during drag
    6. **Swipe left** on a card — card should fly left with red NOPE stamp visible
    7. **Tap Like button** (heart) — card should animate off to the right
    8. **Tap Dislike button** (X) — card should animate off to the left
    9. **Tap Save button** (bookmark) — should show confirmation, card stays on top
    10. **Tap Undo button** — previous card should reappear
    11. Keep swiping until cards run out — should see "You've seen everything!" with refresh button
    12. On mobile (or Chrome DevTools mobile mode): verify swipe gestures work without page scroll interference
    13. Check Network tab: POST /api/feedback calls fire for each swipe action
    14. Refresh page — previously swiped products should NOT reappear in feed
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 6, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `cd apps/stylipp.com && npx tsc --noEmit` passes
- [ ] /feed route renders FeedPage with cards
- [ ] Swipe gestures work on desktop and mobile
- [ ] Like/Dislike/Save/Undo buttons functional
- [ ] Feedback persisted to backend via POST /api/feedback
- [ ] Feed excludes previously interacted products
- [ ] Empty state shown when feed exhausted
- [ ] No console errors or warnings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Human verification approved
- Swipe interface feels responsive and natural
- Feedback pipeline works end-to-end
- Phase 6 complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-swipe-interface-feedback/06-05-SUMMARY.md`

Update STATE.md:
- Current position: Phase 6, Plan 5 of 5, Status: Phase complete
- Add any decisions made during execution
</output>
