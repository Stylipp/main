---
phase: 01-foundation-infrastructure
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [apps/stylipp.com/package.json, apps/stylipp.com/vite.config.ts, apps/stylipp.com/tsconfig.json, apps/stylipp.com/src/main.tsx, apps/stylipp.com/src/App.tsx, apps/stylipp.com/src/theme/theme.ts, apps/stylipp.com/src/types/api.generated.ts, apps/stylipp.com/scripts/generate-types.ts]
---

<objective>
Bootstrap Vite + React + TypeScript PWA with MUI v5 and routing for Stylipp frontend.

Purpose: Create the frontend application foundation with proper tooling, UI library, and navigation structure. This establishes the development environment and component patterns for all future UI work.
Output: Working React app with hot reload, MUI theme, and basic route structure accessible at localhost:5173.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md

**Frontend stack**: React 18 + TypeScript + Vite
**UI library**: MUI v5
**Router**: React Router v6
**Domain**: stylipp.com (will be served here in production)
**Development**: localhost:5173 (Vite default)
**Type generation**: TypeScript types auto-generated from backend OpenAPI spec (packages/schemas → backend → OpenAPI → generated TS)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite + React + TypeScript app</name>
  <files>apps/stylipp.com/package.json, apps/stylipp.com/vite.config.ts, apps/stylipp.com/tsconfig.json, apps/stylipp.com/index.html, apps/stylipp.com/src/main.tsx, apps/stylipp.com/src/App.tsx, apps/stylipp.com/src/vite-env.d.ts</files>
  <action>
    Navigate to apps/ and run: `pnpm create vite web --template react-ts`

    Modify apps/stylipp.com/package.json:
    - name: "@stylipp/web"
    - Add scripts: "dev": "vite", "build": "tsc && vite build", "preview": "vite preview", "lint": "eslint src --ext ts,tsx"

    Modify apps/stylipp.com/tsconfig.json to extend ../../tsconfig.base.json

    Configure apps/stylipp.com/vite.config.ts:
    - Set server.port to 5173
    - Add server.proxy for API calls to http://localhost:8000/api
    - Enable build.sourcemap for debugging

    Test that dev server starts: `cd apps/stylipp.com && pnpm dev`
  </action>
  <verify>cd apps/stylipp.com && pnpm dev starts server at localhost:5173, shows React + Vite welcome page</verify>
  <done>Vite app created, dev server runs without errors, tsconfig extends base config, API proxy configured</done>
</task>

<task type="auto">
  <name>Task 2: Install and configure MUI v5 with theme provider</name>
  <files>apps/stylipp.com/package.json, apps/stylipp.com/src/theme.ts, apps/stylipp.com/src/main.tsx</files>
  <action>
    Install MUI dependencies:
    `cd apps/stylipp.com && pnpm add @mui/material @emotion/react @emotion/styled @mui/icons-material`

    Create apps/stylipp.com/src/theme.ts:
    - Import createTheme from @mui/material/styles
    - Define custom theme with primary color (#1976d2), secondary color, typography (Inter font family)
    - Export theme object

    Modify apps/stylipp.com/src/main.tsx:
    - Wrap App with ThemeProvider and CssBaseline
    - Import theme from ./theme

    Add Inter font to apps/stylipp.com/index.html via Google Fonts CDN link
  </action>
  <verify>Dev server shows MUI CssBaseline applied (normalized styles), no console errors about missing @emotion packages</verify>
  <done>MUI v5 installed, custom theme created with brand colors, ThemeProvider wraps app, Inter font loaded</done>
</task>

<task type="auto">
  <name>Task 3: Set up React Router v6 with basic route structure</name>
  <files>apps/stylipp.com/package.json, apps/stylipp.com/src/App.tsx, apps/stylipp.com/src/pages/HomePage.tsx, apps/stylipp.com/src/pages/NotFoundPage.tsx</files>
  <action>
    Install React Router: `cd apps/stylipp.com && pnpm add react-router-dom`

    Create apps/stylipp.com/src/pages/HomePage.tsx:
    - Simple MUI Container with Typography showing "Stylipp - AI Personal Stylist"
    - Placeholder text: "Welcome to Stylipp. Your personalized fashion discovery starts here."

    Create apps/stylipp.com/src/pages/NotFoundPage.tsx:
    - 404 page with link back to home

    Modify apps/stylipp.com/src/App.tsx:
    - Import BrowserRouter, Routes, Route from react-router-dom
    - Define routes: "/" → HomePage, "*" → NotFoundPage
    - Wrap in BrowserRouter
  </action>
  <verify>Navigate to localhost:5173 shows HomePage, navigate to localhost:5173/invalid shows NotFoundPage with 404 message</verify>
  <done>React Router v6 configured, HomePage and NotFoundPage created, routing works for / and 404 paths</done>
</task>

<task type="auto">
  <name>Task 4: Set up OpenAPI TypeScript type generation</name>
  <files>apps/stylipp.com/package.json, apps/stylipp.com/src/types/api.generated.ts, apps/stylipp.com/scripts/generate-types.ts</files>
  <action>
    Install openapi-typescript for generating TypeScript from OpenAPI:
    `cd apps/stylipp.com && pnpm add -D openapi-typescript`

    Create apps/stylipp.com/scripts/generate-types.ts:
    - Script that fetches http://localhost:8000/openapi.json
    - Generates TypeScript types to src/types/api.generated.ts
    - Can be run with: `pnpm generate:types`

    Add script to apps/stylipp.com/package.json:
    - "generate:types": "npx openapi-typescript http://localhost:8000/openapi.json -o src/types/api.generated.ts"

    Create placeholder apps/stylipp.com/src/types/api.generated.ts:
    - Comment: "// Auto-generated from backend OpenAPI spec"
    - Comment: "// Run `pnpm generate:types` with backend running to regenerate"
    - Export empty object for now (will be populated when backend runs)

    Create apps/stylipp.com/src/types/index.ts:
    - Re-export from ./api.generated
    - Add app-specific types (non-API, UI state types)
  </action>
  <verify>pnpm generate:types script exists in package.json, src/types/ directory structure created</verify>
  <done>OpenAPI type generation configured, placeholder types file created, generate:types script ready for use when backend is running</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/stylipp.com && pnpm dev` starts dev server
- [ ] Browser shows "Stylipp - AI Personal Stylist" at localhost:5173
- [ ] MUI CssBaseline styles applied (no default margins)
- [ ] No console errors in browser devtools
- [ ] Inter font loads from Google Fonts
- [ ] Navigate to /invalid shows 404 page
- [ ] TypeScript compilation succeeds (`pnpm build` in apps/stylipp.com)
- [ ] src/types/api.generated.ts exists with placeholder
- [ ] package.json has "generate:types" script
</verification>

<success_criteria>

- React + Vite app runs with hot reload
- MUI v5 theme applied with custom colors
- React Router configured with home and 404 routes
- TypeScript strict mode enabled
- OpenAPI type generation script configured (will generate when backend runs)
- No build or runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Frontend Bootstrap Summary

**Bootstrapped Vite + React + TypeScript PWA with MUI v5 theme and React Router v6 navigation**

## Accomplishments

- Created Vite + React + TypeScript app in apps/stylipp.com workspace
- Configured MUI v5 with custom theme (primary color, Inter font)
- Set up React Router v6 with HomePage and NotFoundPage
- Configured Vite dev server with API proxy to localhost:8000
- Set up OpenAPI TypeScript type generation from backend

## Files Created/Modified

- `apps/stylipp.com/package.json` - Frontend dependencies + generate:types script
- `apps/stylipp.com/vite.config.ts` - Vite configuration with API proxy
- `apps/stylipp.com/src/theme/theme.ts` - MUI custom theme
- `apps/stylipp.com/src/App.tsx` - Router setup
- `apps/stylipp.com/src/pages/HomePage.tsx` - Landing page
- `apps/stylipp.com/src/pages/NotFoundPage.tsx` - 404 page
- `apps/stylipp.com/src/types/api.generated.ts` - Placeholder for generated API types
- `apps/stylipp.com/src/types/index.ts` - Type exports

## Decisions Made

- Using Vite (not CRA) for faster HMR and build times
- MUI v5 for production-ready accessible components
- Inter font for modern, readable typography
- API proxy at /api routes to backend on port 8000
- TypeScript types generated from OpenAPI (packages/schemas → backend → OpenAPI → frontend types)

## Issues Encountered

None

## Next Step

Ready for 01-03-PLAN.md (Backend Bootstrap)
</output>
