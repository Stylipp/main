# ===========================================
# Stylipp Production Environment Variables
# ===========================================
# Copy this file to .env and fill in real values:
#   cp .env.example .env
#
# This file is used by: docker compose -f docker-compose.yml
# Location on server: /opt/stylipp/infra/.env (or wherever DEPLOY_PATH is)
# NEVER commit the real .env to git.

# -------------------------------------------
# PostgreSQL
# -------------------------------------------
DB_NAME=stylipp
DB_USER=stylipp
DB_PASSWORD=                    # Generate: openssl rand -base64 32

# -------------------------------------------
# Redis
# -------------------------------------------
REDIS_PASSWORD=                 # Generate: openssl rand -base64 32

# -------------------------------------------
# JWT Authentication
# -------------------------------------------
# Keys are loaded from PEM files, NOT env vars.
# Ensure these files exist on the server:
#   secrets/jwt/private.pem
#   secrets/jwt/public.pem
# (mounted read-only into the container by docker-compose)
#
# To generate a new key pair:
#   openssl ecparam -genkey -name prime256v1 -noout -out secrets/jwt/private.pem
#   openssl ec -in secrets/jwt/private.pem -pubout -out secrets/jwt/public.pem

# -------------------------------------------
# S3-Compatible Object Storage (Hetzner)
# -------------------------------------------
S3_ENDPOINT_URL=https://hel1.your-objectstorage.com
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET_NAME=stylipp-uploads

# -------------------------------------------
# Docker Image Tags
# -------------------------------------------
GITHUB_REPOSITORY_OWNER=stylipp
TAG=latest

# -------------------------------------------
# Environment
# -------------------------------------------
ENVIRONMENT=production
